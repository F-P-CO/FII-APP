name: fii_app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.0
  flutter_mobx: ^2.0.1
  get_it: ^7.2.0
  html: ^0.15.0
  http: ^0.13.3
  mobx: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.6
  lint: ^1.0.0
  mobx_codegen: ^2.0.2

flutter:
  uses-material-design: true
